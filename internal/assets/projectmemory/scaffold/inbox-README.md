# Memory Inbox

Raw, unprocessed notes land here. Review periodically and promote with:

    squadai memory promote docs/memory/_inbox/<filename>

Notes are promoted to `decisions/`, `learnings/`, or `incidents/`.
