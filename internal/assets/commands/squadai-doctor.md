Run `squadai doctor --json` and display a pre-flight diagnostics report.

Group checks by category (environment, agents, config, MCP, filesystem, drift).
For any failing checks, show the error message and suggested fix.
If all checks pass, confirm the environment is healthy.
