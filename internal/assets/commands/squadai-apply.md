Run `squadai apply --json` to apply the current SquadAI configuration.

Show the apply report with each step's status (ok / FAIL / SKIP).
If there are failures, show the error details and the backup ID for recovery.
After a successful apply, confirm by running `squadai verify`.
