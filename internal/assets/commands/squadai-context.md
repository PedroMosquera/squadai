Run `squadai context --format prompt` and display the SquadAI project context.

This gives a summary of the current configuration: adapters, agents, MCP servers,
methodology, and model tier. Use this to understand the current project setup
before making changes or answering questions about the AI agent configuration.
