Run `squadai verify --json` and summarize the compliance and health report.

Group results by component. Highlight any failing checks in red and suggest fixes.
If all checks pass, confirm the environment is compliant.
Include a drift check by also running `squadai verify --strict --json`.
