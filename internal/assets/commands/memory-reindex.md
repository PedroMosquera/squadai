---
description: Regenerate the project memory index after manual edits
---

Regenerate the project memory index.

Run:

```bash
squadai memory reindex
```

Report the number of entries indexed from the command output.
Use this after manually adding, editing, or moving note files in `docs/memory/`.
