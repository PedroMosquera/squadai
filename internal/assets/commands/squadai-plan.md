Run `squadai plan --json` and show what would change if apply were run.

Display the results as a formatted table grouped by adapter and component.
If there are no planned changes, confirm the config is already up to date.
