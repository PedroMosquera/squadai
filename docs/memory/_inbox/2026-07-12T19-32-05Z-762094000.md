WS2 (QUALITY_PLAN.md) done on branch worktree-ws2-fix-skip-tests, commit f7b7070: all 19 conditional t.Skip calls in internal/planner/render_test.go, internal/tui/tui_test.go, internal/pipeline/executor_test.go converted to hard failures. Key finding: none of the skips ever fired at runtime — fixtures already produced every action, so the skips were pure latent false-coverage traps and no fixture changes were needed. TUI skill catalog is embedded (skills/catalog.json), so those guards were deterministic, not environment skips. WS3 (god-file split) is now unblocked.