Codebase evaluation (2026-07-12) produced .planning/QUALITY_PLAN.md: 5 quality workstreams — WS1 honest token/cost reporting (tokenizer fallback labeling, pricing as data), WS2 remove 19 false-coverage t.Skip tests in planner/tui/pipeline, WS3 split commands.go (5.5k lines) and tui.go after WS2, WS4 dedupe adapter APPDATA path resolution, WS5 plugin install guardrails (confirm, SHA pin, audit log). TF-IDF rewrite explicitly deferred. Do WS2 before WS3.