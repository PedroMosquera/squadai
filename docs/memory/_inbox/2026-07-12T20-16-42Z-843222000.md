Quality plan complete (2026-07-12): all five QUALITY_PLAN.md workstreams merged to main at 0034473 (+cleanup e8c9505). WS1 honest tokens (embedded pricing.json with generated_at + divisors, Lookup ok-bool, ~ approx markers, removed dishonest claude->o200k proxy), WS2 skip-to-fail tests, WS3 pure-move splits (commands.go -> 18 files max 522 lines, tui.go -> 7 files max 661), WS4 shared adapters/paths.UserConfigDir, WS5 plugin guardrails (staged quarantine installs, --yes/non-TTY fail-closed, git:repo@sha pinning, plugin:install/remove audit events, plugins remove-git, TUI exec validated against embedded catalog). Gotchas: the [pi] auto-committer had swept .claude/worktrees/* gitlinks into main — removed and .claude/worktrees/ is now gitignored; token-usage counts stay unprefixed because they are API-reported exact values.